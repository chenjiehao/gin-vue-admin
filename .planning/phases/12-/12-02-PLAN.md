---
phase: "12"
plan: "02"
type: execute
wave: 2
depends_on: []
files_modified:
  - server/service/system/sys_data_source.go
  - server/api/v1/system/data_source.go
  - server/router/system/data_source.go
autonomous: true
requirements:
  - DS-04
  - DS-05
  - DS-06
must_haves:
  truths:
    - "User can batch delete multiple data sources with confirmation"
    - "User can batch enable/disable multiple data sources with confirmation"
    - "Batch operations return success count or failure count"
  artifacts:
    - path: server/service/system/sys_data_source.go
      provides: BatchDelete, BatchUpdateStatus methods
      contains: BatchDelete, BatchUpdateStatus
    - path: server/api/v1/system/data_source.go
      provides: BatchDelete, BatchUpdateStatus API endpoints
      exports: BatchDelete, BatchUpdateStatus
    - path: server/router/system/data_source.go
      provides: Route registration for batch operations
      contains: batchDelete, batchUpdateStatus
  key_links:
    - from: server/api/v1/system/data_source.go
      to: server/service/system/sys_data_source.go
      via: dataSourceServiceApp.BatchDelete, BatchUpdateStatus
      pattern: dataSourceService.*Batch(Delete|UpdateStatus)
---

<objective>
实现数据源批量操作功能（后端）。支持批量删除和批量启用/禁用状态，由前端传入 ID 列表，后端批量处理并返回成功/失败数量。
</objective>

<context>
@server/service/system/sys_data_source.go
@server/api/v1/system/data_source.go
@server/router/system/data_source.go
@server/model/common/request/common.go

## IdsReq (server/model/common/request/common.go)
```go
type IdsReq struct {
    Ids []int `json:"ids" form:"ids"`
}
```

## Existing DataSourceService
```go
func (dataSourceService *DataSourceService) DeleteDataSource(id uint) error
func (dataSourceService *DataSourceService) UpdateDataSource(dataSource *system.SysDataSource) error
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: 实现 BatchDelete Service 方法</name>
  <files>server/service/system/sys_data_source.go</files>
  <action>
在 `server/service/system/sys_data_source.go` 末尾添加两个方法：

```go
// BatchDelete 批量删除数据源
// @param ids []uint
// @return successCount int, failCount int, err error
func (dataSourceService *DataSourceService) BatchDelete(ids []uint) (successCount int, failCount int, err error) {
    if len(ids) == 0 {
        return 0, 0, nil
    }

    tx := global.GVA_DB.Delete(&system.SysDataSource{}, "id IN ?", ids)
    if tx.Error != nil {
        return 0, len(ids), tx.Error
    }

    successCount = int(tx.RowsAffected)
    failCount = len(ids) - successCount
    return successCount, failCount, nil
}

// BatchUpdateStatus 批量更新数据源状态
// @param ids []uint, status uint
// @return successCount int, failCount int, err error
func (dataSourceService *DataSourceService) BatchUpdateStatus(ids []uint, status uint) (successCount int, failCount int, err error) {
    if len(ids) == 0 {
        return 0, 0, nil
    }

    tx := global.GVA_DB.Model(&system.SysDataSource{}).Where("id IN ?", ids).Update("status", status)
    if tx.Error != nil {
        return 0, len(ids), tx.Error
    }

    successCount = int(tx.RowsAffected)
    failCount = len(ids) - successCount
    return successCount, failCount, nil
}
```
</action>
  <verify>
cd /Users/chenjiehao/Documents/vibecoding/gin-vue-admin/server && go build ./... 2>&1 | head -30
</verify>
  <done>BatchDelete(ids []uint) 和 BatchUpdateStatus(ids []uint, status uint) 方法已实现</done>
</task>

<task type="auto">
  <name>Task 2: 实现 BatchDelete 和 BatchUpdateStatus API</name>
  <files>server/api/v1/system/data_source.go</files>
  <action>
在 `server/api/v1/system/data_source.go` 末尾添加两个 API 方法：

```go
// BatchDelete
// @Tags      DataSource
// @Summary   批量删除数据源
// @Security  ApiKeyAuth
// @accept    application/json
// @Produce   application/json
// @Param     data  body      request.IdsReq                   true  "ID列表"
// @Success   200   {object}  response.Response{data=map[string]interface{},msg=string}  "删除结果"
// @Router    /dataSource/batchDelete [post]
func (d *DataSourceApi) BatchDelete(c *gin.Context) {
    var req request.IdsReq
    err := c.ShouldBindJSON(&req)
    if err != nil {
        response.FailWithMessage(err.Error(), c)
        return
    }
    if len(req.Ids) == 0 {
        response.FailWithMessage("未选择数据源", c)
        return
    }

    ids := make([]uint, len(req.Ids))
    for i, id := range req.Ids {
        ids[i] = uint(id)
    }

    successCount, failCount, err := dataSourceService.BatchDelete(ids)
    if err != nil {
        global.GVA_LOG.Error("批量删除失败!", zap.Error(err))
        response.FailWithDetailed(map[string]interface{}{
            "successCount": successCount,
            "failCount":    failCount + 1, // include this error as 1 failure
        }, "批量删除失败", c)
        return
    }

    response.OkWithDetailed(map[string]interface{}{
        "successCount": successCount,
        "failCount":    failCount,
    }, fmt.Sprintf("成功删除 %d 个数据源", successCount), c)
}

// BatchUpdateStatus
// @Tags      DataSource
// @Summary   批量更新数据源状态
// @Security  ApiKeyAuth
// @accept    application/json
// @Produce   application/json
// @Param     data  body      request.BatchUpdateStatusReq      true  "ID列表和状态"
// @Success   200   {object}  response.Response{data=map[string]interface{},msg=string}  "更新结果"
// @Router    /dataSource/batchUpdateStatus [post]
func (d *DataSourceApi) BatchUpdateStatus(c *gin.Context) {
    var req request.BatchUpdateStatusReq
    err := c.ShouldBindJSON(&req)
    if err != nil {
        response.FailWithMessage(err.Error(), c)
        return
    }
    if len(req.Ids) == 0 {
        response.FailWithMessage("未选择数据源", c)
        return
    }

    ids := make([]uint, len(req.Ids))
    for i, id := range req.Ids {
        ids[i] = uint(id)
    }

    successCount, failCount, err := dataSourceService.BatchUpdateStatus(ids, req.Status)
    if err != nil {
        global.GVA_LOG.Error("批量更新状态失败!", zap.Error(err))
        response.FailWithDetailed(map[string]interface{}{
            "successCount": successCount,
            "failCount":    failCount + 1,
        }, "批量更新状态失败", c)
        return
    }

    statusText := "启用"
    if req.Status == 0 {
        statusText = "禁用"
    }
    response.OkWithDetailed(map[string]interface{}{
        "successCount": successCount,
        "failCount":    failCount,
    }, fmt.Sprintf("成功 %s %d 个数据源", statusText, successCount), c)
}
```

同时在文件顶部添加 `fmt` import（如果尚未导入）。
</action>
  <verify>
cd /Users/chenjiehao/Documents/vibecoding/gin-vue-admin/server && go build ./... 2>&1 | head -30
</verify>
  <done>BatchDelete 和 BatchUpdateStatus API 已注册</done>
</task>

<task type="auto">
  <name>Task 3: 创建 BatchUpdateStatusReq 请求模型</name>
  <files>server/model/common/request/common.go</files>
  <action>
在 `server/model/common/request/common.go` 的 `IdsReq` 之后添加：

```go
// BatchUpdateStatusReq 批量更新状态请求
type BatchUpdateStatusReq struct {
    Ids    []int `json:"ids" form:"ids"`
    Status uint  `json:"status" form:"status"`
}
```
</action>
  <verify>
cd /Users/chenjiehao/Documents/vibecoding/gin-vue-admin/server && go build ./... 2>&1 | head -30
</verify>
  <done>BatchUpdateStatusReq 类型已定义并可用于 API 参数绑定</done>
</task>

<task type="auto">
  <name>Task 4: 注册 batchDelete 和 batchUpdateStatus 路由</name>
  <files>server/router/system/data_source.go</files>
  <action>
在 `server/router/system/data_source.go` 的 `InitDataSourceRouter` 中添加新路由：

```go
dataSourceRouter.POST("batchDelete", dataSourceApi.BatchDelete)
dataSourceRouter.POST("batchUpdateStatus", dataSourceApi.BatchUpdateStatus)
```
</action>
  <verify>
cd /Users/chenjiehao/Documents/vibecoding/gin-vue-admin/server && go build ./... 2>&1 | head -30
</verify>
  <done>路由 POST /dataSource/batchDelete 和 POST /dataSource/batchUpdateStatus 已注册</done>
</task>

</tasks>

<verification>
- [ ] `go build ./...` 成功，无编译错误
- [ ] BatchUpdateStatusReq 类型在 server/model/common/request/common.go 中定义
- [ ] 路由包含 batchDelete 和 batchUpdateStatus
</verification>

<success_criteria>
- BatchDelete API 接收 ids 列表，批量删除并返回成功/失败数量
- BatchUpdateStatus API 接收 ids 列表和 status，批量更新状态并返回成功/失败数量
- 后端服务层使用 GORM 的 Where("id IN ?") 进行批量操作
</success_criteria>

<output>
After completion, create `.planning/phases/12-/12-02-SUMMARY.md`
</output>