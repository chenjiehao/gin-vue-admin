---
phase: "12"
plan: "03"
type: execute
wave: 3
depends_on:
  - "12-01"
  - "12-02"
files_modified:
  - web/src/api/dataSource.js
  - web/src/view/dataIntegration/dataSource.vue
autonomous: true
requirements:
  - DS-01
  - DS-02
  - DS-03
  - DS-04
  - DS-05
  - DS-06
  - DS-07
must_haves:
  truths:
    - "User can test a data source connection from the form and see detailed result"
    - "User can select multiple rows in the table via checkboxes"
    - "User can batch delete selected data sources with confirmation dialog"
    - "User can batch enable/disable selected data sources with confirmation dialog"
    - "Batch operations show success count or failure count after completion"
  artifacts:
    - path: web/src/api/dataSource.js
      provides: testConnection, batchDelete, batchUpdateStatus API functions
      contains: testConnection, batchDelete, batchUpdateStatus
    - path: web/src/view/dataIntegration/dataSource.vue
      provides: Test connection button, multi-select table, batch operation buttons
      contains: el-table-column.*selection, 测试连接, 批量删除, 批量启用, 批量禁用
  key_links:
    - from: web/src/view/dataIntegration/dataSource.vue
      to: web/src/api/dataSource.js
      via: testConnection(), batchDelete(), batchUpdateStatus()
      pattern: import.*testConnection.*from.*dataSource
---

<objective>
实现数据源管理的增强功能前端 UI。包括：连接测试按钮、批量操作按钮（批量删除、批量启用、批量禁用）、多选表格、确认对话框。
</objective>

<context>
@web/src/view/dataIntegration/dataSource.vue
@web/src/api/dataSource.js

## Existing dataSource.vue Key Sections
- Table with `ref="multipleTable"` and `@selection-change="handleSelectionChange"`
- Form with `ref="dataSourceFormRef"`, dataSourceForm, dataSourceRules
- Button row in table toolbar: `goCreate`, `getTableData` refresh
- Existing APIs: createDataSource, updateDataSource, deleteDataSource, getDataSourceList

## Existing dataSource.js API Pattern
```javascript
export const createDataSource = (data) => {
  return service({ url: '/dataSource/createDataSource', method: 'post', data })
}
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: 添加 testConnection、batchDelete、batchUpdateStatus API</name>
  <files>web/src/api/dataSource.js</files>
  <action>
在 `web/src/api/dataSource.js` 末尾添加三个新的 API 函数：

```javascript
// @Tags DataSource
// @Summary 测试数据源连接
// @Security ApiKeyAuth
// @accept application/json
// @Produce application/json
// @Param data body system.SysDataSource true "数据源信息"
// @Success 200 {string} string "{"code":0,"data":{},"msg":"连接成功"}"
// @Router /dataSource/testConnection [post]
export const testConnection = (data) => {
  return service({
    url: '/dataSource/testConnection',
    method: 'post',
    data
  })
}

// @Tags DataSource
// @Summary 批量删除数据源
// @Security ApiKeyAuth
// @accept application/json
// @Produce application/json
// @Param data body request.IdsReq true "ID列表"
// @Success 200 {string} string "{"code":0,"data":{},"msg":"删除成功"}"
// @Router /dataSource/batchDelete [post]
export const batchDelete = (data) => {
  return service({
    url: '/dataSource/batchDelete',
    method: 'post',
    data
  })
}

// @Tags DataSource
// @Summary 批量更新数据源状态
// @Security ApiKeyAuth
// @accept application/json
// @Produce application/json
// @Param data body request.BatchUpdateStatusReq true "ID列表和状态"
// @Success 200 {string} string "{"code":0,"data":{},"msg":"更新成功"}"
// @Router /dataSource/batchUpdateStatus [post]
export const batchUpdateStatus = (data) => {
  return service({
    url: '/dataSource/batchUpdateStatus',
    method: 'post',
    data
  })
}
```
</action>
  <verify>
ls -la /Users/chenjiehao/Documents/vibecoding/gin-vue-admin/web/src/api/dataSource.js && head -10 /Users/chenjiehao/Documents/vibecoding/gin-vue-admin/web/src/api/dataSource.js
</verify>
  <done>dataSource.js 包含 testConnection, batchDelete, batchUpdateStatus 三个 API 函数</done>
</task>

<task type="auto">
  <name>Task 2: 在 dataSource.vue 中添加连接测试按钮</name>
  <files>web/src/view/dataIntegration/dataSource.vue</files>
  <action>
在 `web/src/view/dataIntegration/dataSource.vue` 中进行以下修改：

**1. 导入新 API：**
在 script setup 的 import 语句中添加：
```javascript
import { getDataSourceList, deleteDataSource as apiDeleteDataSource, createDataSource, updateDataSource, testConnection } from '@/api/dataSource'
```

**2. 在表单的"保存按钮"后添加"测试连接"按钮：**
```vue
<el-button type="primary" @click="saveDataSource">{{ isEdit ? '保存修改' : '创建数据源' }}</el-button>
<el-button type="success" @click="handleTestConnection" :loading="testingConnection">测试连接</el-button>
<el-button @click="closeForm">取消</el-button>
```

**3. 添加测试连接相关的状态和方法：**
在 script setup 中添加状态：
```javascript
const testingConnection = ref(false)
```

添加方法：
```javascript
// 测试连接
const handleTestConnection = async () => {
  if (!dataSourceFormRef.value) return
  await dataSourceFormRef.value.validateField(['name', 'type', 'host', 'port', 'database'], async (valid) => {
    if (valid) {
      testingConnection.value = true
      try {
        const res = await testConnection(dataSourceForm.value)
        if (res.code === 0) {
          const data = res.data || {}
          if (data.success) {
            ElMessage({ type: 'success', message: data.message || '连接成功' })
          } else {
            ElMessage({ type: 'error', message: data.message || '连接失败' })
          }
        } else {
          ElMessage({ type: 'error', message: res.msg || '测试连接失败' })
        }
      } catch (e) {
        ElMessage({ type: 'error', message: '测试连接失败' })
      } finally {
        testingConnection.value = false
      }
    }
  })
}
```
</action>
  <verify>
grep -n "testConnection\|handleTestConnection\|testingConnection" /Users/chenjiehao/Documents/vibecoding/gin-vue-admin/web/src/view/dataIntegration/dataSource.vue
</verify>
  <done>表单中显示"测试连接"按钮，点击后调用后端 API 并根据返回结果显示成功/失败消息</done>
</task>

<task type="auto">
  <name>Task 3: 在 dataSource.vue 中添加多选和批量操作按钮</name>
  <files>web/src/view/dataIntegration/dataSource.vue</files>
  <action>
在 `web/src/view/dataIntegration/dataSource.vue` 中进行以下修改：

**1. 在表格中添加多选列：**
在 `<el-table>` 开头添加：
```vue
<el-table-column type="selection" width="55" align="left" />
```

**2. 替换表格上方的按钮区域：**
原：
```vue
<div class="gva-btn-list">
  <el-button icon="plus" type="primary" @click="goCreate">新增数据源</el-button>
  <el-button icon="refresh" @click="getTableData">刷新</el-button>
</div>
```

改为：
```vue
<div class="gva-btn-list">
  <el-button icon="plus" type="primary" @click="goCreate">新增数据源</el-button>
  <el-button icon="refresh" @click="getTableData">刷新</el-button>
  <span class="selection-count" v-if="multipleSelection.length > 0">已选择 {{ multipleSelection.length }} 项</span>
  <el-button icon="delete" type="danger" @click="handleBatchDelete" :disabled="multipleSelection.length === 0">批量删除</el-button>
  <el-button icon="check" type="success" @click="handleBatchEnable" :disabled="multipleSelection.length === 0">批量启用</el-button>
  <el-button icon="close" type="warning" @click="handleBatchDisable" :disabled="multipleSelection.length === 0">批量禁用</el-button>
</div>
```

**3. 添加批量操作方法：**
在 script setup 中添加：
```javascript
// 批量删除
const handleBatchDelete = async () => {
  if (multipleSelection.value.length === 0) {
    ElMessage({ type: 'warning', message: '请先选择要删除的数据源' })
    return
  }
  ElMessageBox.confirm(`确定要删除选中的 ${multipleSelection.value.length} 个数据源吗？`, '提示', {
    confirmButtonText: '确定',
    cancelButtonText: '取消',
    type: 'warning'
  }).then(async () => {
    const ids = multipleSelection.value.map(item => item.id)
    try {
      const res = await batchDelete({ ids })
      if (res.code === 0) {
        const data = res.data || {}
        ElMessage({ type: 'success', message: `成功删除 ${data.successCount || 0} 个数据源` })
        getTableData()
        multipleSelection.value = []
      } else {
        ElMessage({ type: 'error', message: res.msg || '批量删除失败' })
      }
    } catch (e) {
      ElMessage({ type: 'error', message: '批量删除失败' })
    }
  }).catch(() => {})
}

// 批量启用
const handleBatchEnable = async () => {
  if (multipleSelection.value.length === 0) {
    ElMessage({ type: 'warning', message: '请先选择要启用的数据源' })
    return
  }
  ElMessageBox.confirm(`确定要启用选中的 ${multipleSelection.value.length} 个数据源吗？`, '提示', {
    confirmButtonText: '确定',
    cancelButtonText: '取消',
    type: 'warning'
  }).then(async () => {
    const ids = multipleSelection.value.map(item => item.id)
    try {
      const res = await batchUpdateStatus({ ids, status: 1 })
      if (res.code === 0) {
        const data = res.data || {}
        ElMessage({ type: 'success', message: `成功启用 ${data.successCount || 0} 个数据源` })
        getTableData()
        multipleSelection.value = []
      } else {
        ElMessage({ type: 'error', message: res.msg || '批量启用失败' })
      }
    } catch (e) {
      ElMessage({ type: 'error', message: '批量启用失败' })
    }
  }).catch(() => {})
}

// 批量禁用
const handleBatchDisable = async () => {
  if (multipleSelection.value.length === 0) {
    ElMessage({ type: 'warning', message: '请先选择要禁用的数据源' })
    return
  }
  ElMessageBox.confirm(`确定要禁用选中的 ${multipleSelection.value.length} 个数据源吗？`, '提示', {
    confirmButtonText: '确定',
    cancelButtonText: '取消',
    type: 'warning'
  }).then(async () => {
    const ids = multipleSelection.value.map(item => item.id)
    try {
      const res = await batchUpdateStatus({ ids, status: 0 })
      if (res.code === 0) {
        const data = res.data || {}
        ElMessage({ type: 'success', message: `成功禁用 ${data.successCount || 0} 个数据源` })
        getTableData()
        multipleSelection.value = []
      } else {
        ElMessage({ type: 'error', message: res.msg || '批量禁用失败' })
      }
    } catch (e) {
      ElMessage({ type: 'error', message: '批量禁用失败' })
    }
  }).catch(() => {})
}
```

**4. 导入新 API 和 ElMessageBox：**
确保 import 语句包含：
```javascript
import { getDataSourceList, deleteDataSource as apiDeleteDataSource, createDataSource, updateDataSource, testConnection, batchDelete, batchUpdateStatus } from '@/api/dataSource'
import { ElMessage, ElMessageBox } from 'element-plus'
```

**5. 添加样式：**
在 style 中添加：
```css
.selection-count {
  margin-left: 16px;
  color: #909399;
  font-size: 14px;
}
```
</action>
  <verify>
grep -n "handleBatchDelete\|handleBatchEnable\|handleBatchDisable\|batchDelete\|batchUpdateStatus" /Users/chenjiehao/Documents/vibecoding/gin-vue-admin/web/src/view/dataIntegration/dataSource.vue | head -20
</verify>
  <done>表格支持多选，批量操作按钮显示在工具栏，弹窗确认后执行批量删除/启用/禁用</done>
</task>

</tasks>

<verification>
- [ ] `grep testConnection web/src/api/dataSource.js` 返回 testConnection 函数定义
- [ ] `grep batchDelete web/src/api/dataSource.js` 返回 batchDelete 函数定义
- [ ] `grep "el-table-column.*selection" web/src/view/dataIntegration/dataSource.vue` 返回多选列
- [ ] `grep "批量删除\|批量启用\|批量禁用" web/src/view/dataIntegration/dataSource.vue` 返回批量按钮
</verification>

<success_criteria>
- 连接测试按钮在表单中显示，点击后显示 loading 状态并返回成功/失败消息
- 表格第一列为多选框，支持 shift 多选
- 批量操作按钮在表格右上角，显示已选数量
- 批量操作前有确认对话框，显示选中数量
- 操作完成后刷新列表并清空选择
</success_criteria>

<output>
After completion, create `.planning/phases/12-/12-03-SUMMARY.md`
</output>