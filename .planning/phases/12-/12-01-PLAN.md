---
phase: "12"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - server/service/system/sys_data_source.go
  - server/api/v1/system/data_source.go
  - server/router/system/data_source.go
autonomous: true
requirements:
  - DS-01
  - DS-02
  - DS-03
must_haves:
  truths:
    - "User can test a data source connection and see success/failure with details"
    - "Connection test uses real database drivers to establish actual connections"
  artifacts:
    - path: server/service/system/sys_data_source.go
      provides: TestConnection method with driver-specific logic
      contains: TestConnection func
    - path: server/api/v1/system/data_source.go
      provides: TestConnection API endpoint
      exports: TestConnection
    - path: server/router/system/data_source.go
      provides: Route registration for testConnection
      contains: dataSourceRouter.POST.*testConnection
  key_links:
    - from: server/api/v1/system/data_source.go
      to: server/service/system/sys_data_source.go
      via: dataSourceServiceApp.TestConnection
      pattern: dataSourceService.*TestConnection
---

<objective>
实现数据源连接测试功能（后端）。用户点击"测试连接"按钮时，前端调用后端 API，后端使用真实的数据库驱动尝试建立连接，返回成功或详细错误信息。
</objective>

<context>
@web/src/view/dataIntegration/dataSource.vue
@server/model/system/sys_datasource.go

## SysDataSource Model (server/model/system/sys_datasource.go)
```go
type SysDataSource struct {
    global.GVA_MODEL
    Name     string `json:"name" gorm:"comment:数据源名称"`
    Type     string `json:"type" gorm:"comment:数据源类型"` // MySQL/PostgreSQL/达梦/人大金仓/Oracle/SQLServer
    Host     string `json:"host" gorm:"comment:主机地址"`
    Port     int    `json:"port" gorm:"comment:端口"`
    Database string `json:"database" gorm:"comment:数据库名称"`
    Username string `json:"username" gorm:"comment:用户名"`
    Password string `json:"password" gorm:"comment:密码"`
    Status   uint   `json:"status" gorm:"default:1;comment:状态 0禁用 1启用"`
    Remark   string `json:"remark" gorm:"comment:备注"`
}
```

## Existing DataSourceService (server/service/system/sys_data_source.go)
```go
var DataSourceServiceApp = new(DataSourceService)

func (dataSourceService *DataSourceService) GetDataSourceById(id uint) (dataSource *system.SysDataSource, err error)
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: 实现 TestConnection Service 方法</name>
  <files>server/service/system/sys_data_source.go</files>
  <action>
在 `server/service/system/sys_data_source.go` 末尾添加 `TestConnection` 方法：

```go
// TestConnection 测试数据源连接
// @param dataSource *system.SysDataSource
// @return success bool, message string
func (dataSourceService *DataSourceService) TestConnection(dataSource *system.SysDataSource) (bool, string) {
    var dsn string
    var driver gorm.Dialector

    switch dataSource.Type {
    case "MySQL":
        dsn = fmt.Sprintf("%s:%s@tcp(%s:%d)/%s?charset=utf8mb4&parseTime=True&loc=Local",
            dataSource.Username, dataSource.Password, dataSource.Host, dataSource.Port, dataSource.Database)
        driver = mysql.Open(dsn)
    case "PostgreSQL":
        dsn = fmt.Sprintf("host=%s user=%s password=%s dbname=%s port=%d sslmode=disable",
            dataSource.Host, dataSource.Username, dataSource.Password, dataSource.Database, dataSource.Port)
        driver = postgres.Open(dsn)
    case "SQLServer":
        dsn = fmt.Sprintf("sqlserver://%s:%s@%s:%d?database=%s",
            dataSource.Username, dataSource.Password, dataSource.Host, dataSource.Port, dataSource.Database)
        driver = mssql.Open(dsn)
    case "达梦", "人大金仓", "Oracle":
        // 暂不支持，显示友好错误
        return false, "暂不支持 " + dataSource.Type + " 类型的连接测试"
    default:
        return false, "未知的数据源类型: " + dataSource.Type
    }

    db, err := gorm.Open(driver, &gorm.Config{})
    if err != nil {
        return false, "连接失败: " + err.Error()
    }

    sqlDB, err := db.DB()
    if err != nil {
        return false, "获取数据库对象失败: " + err.Error()
    }

    ctx, cancel := context.WithTimeout(context.Background(), 10*time.Second)
    defer cancel()

    if err := sqlDB.PingContext(ctx); err != nil {
        return false, "Ping 失败: " + err.Error()
    }

    sqlDB.Close()
    return true, "连接成功"
}
```

需要添加 import：
```go
import (
    "context"
    "fmt"
    "time"

    "gorm.io/driver/mysql"
    "gorm.io/driver/postgres"
    "gorm.io/driver/sqlserver"
    "gorm.io/gorm"
)
```
</action>
  <verify>
cd /Users/chenjiehao/Documents/vibecoding/gin-vue-admin/server && go build ./... 2>&1 | head -30
</verify>
  <done>TestConnection 方法可接受 SysDataSource 参数，返回 (bool, string)，支持 MySQL/PostgreSQL/SQLServer 三种数据库的连接测试</done>
</task>

<task type="auto">
  <name>Task 2: 实现 TestConnection API</name>
  <files>server/api/v1/system/data_source.go</files>
  <action>
在 `server/api/v1/system/data_source.go` 末尾添加：

```go
// TestConnection
// @Tags      DataSource
// @Summary   测试数据源连接
// @Security  ApiKeyAuth
// @accept    application/json
// @Produce   application/json
// @Param     data  body      system.SysDataSource           true  "数据源信息"
// @Success   200   {object}  response.Response{data=map[string]interface{},msg=string}  "测试结果"
// @Router    /dataSource/testConnection [post]
func (d *DataSourceApi) TestConnection(c *gin.Context) {
    var dataSource system.SysDataSource
    err := c.ShouldBindJSON(&dataSource)
    if err != nil {
        response.FailWithMessage(err.Error(), c)
        return
    }
    success, message := dataSourceService.TestConnection(&dataSource)
    if success {
        response.OkWithDetailed(map[string]interface{}{"success": true, "message": message}, "连接成功", c)
    } else {
        response.FailWithDetailed(map[string]interface{}{"success": false, "message": message}, message, c)
    }
}
```
</action>
  <verify>
cd /Users/chenjiehao/Documents/vibecoding/gin-vue-admin/server && go build ./... 2>&1 | head -30
</verify>
  <done>TestConnection API 注册到 POST /dataSource/testConnection，返回 {success, message}</done>
</task>

<task type="auto">
  <name>Task 3: 注册 testConnection 路由</name>
  <files>server/router/system/data_source.go</files>
  <action>
在 `server/router/system/data_source.go` 的 `InitDataSourceRouter` 中添加新路由：

```go
dataSourceRouter.POST("testConnection", dataSourceApi.TestConnection)
```

确保在 `dataSourceRouter.POST("getDataSourceById"...)` 之后添加。
</action>
  <verify>
cd /Users/chenjiehao/Documents/vibecoding/gin-vue-admin/server && go build ./... 2>&1 | head -30
</verify>
  <done>路由 POST /dataSource/testConnection 已注册到 DataSourceRouter</done>
</task>

</tasks>

<verification>
- [ ] `go build ./...` 成功，无编译错误
- [ ] TestConnection 方法导入 gorm.io/driver/mysql, postgres, sqlserver
- [ ] API 路径为 /dataSource/testConnection
</verification>

<success_criteria>
- 后端 TestConnection 方法支持 MySQL、PostgreSQL、SQLServer 三种数据库类型
- 连接超时时间设置为 10 秒
- 失败时返回详细错误信息（驱动错误、Ping 失败等）
- API 返回格式为 `{success: bool, message: string}`
</success_criteria>

<output>
After completion, create `.planning/phases/12-/12-01-SUMMARY.md`
</output>