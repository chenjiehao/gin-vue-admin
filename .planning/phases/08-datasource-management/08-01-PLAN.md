---
name: 08-01-数据源管理
description: 在数据集成菜单下新建数据源管理二级菜单，记录可访问的数据源信息
phase: 8
plan: 1
status: completed
execution_context:
  directory: .planning/phases/08-datasource-management
  wave: 1
  waves_total: 1
gap_closure: false
source: null
completed: 2026-04-22
---

## Goal

在"数据集成"一级菜单下，新建"数据源管理"二级菜单，用于记录系统中可以被访问的数据源信息（MySQL、达梦、人大金仓、PG等）。

## Completed Tasks

### Task 1: 创建后端数据模型
- [x] 创建 `server/model/system/sys_datasource.go`
- [x] 添加到 `server/initialize/gorm.go` AutoMigrate

### Task 2: 创建 Service 层
- [x] 创建 `server/service/system/sys_data_source.go`
- [x] 添加 DataSourceService 到 `server/service/system/enter.go`

### Task 3: 创建 API 控制器
- [x] 创建 `server/api/v1/system/data_source.go`
- [x] 添加 DataSourceApi 到 `server/api/v1/system/enter.go`
- [x] 创建 `server/router/system/data_source.go`
- [x] 添加 DataSourceRouter 到 `server/router/system/enter.go`
- [x] 注册路由到 `server/initialize/router.go`

### Task 4: 创建前端页面
- [x] 创建 `web/src/api/dataSource.js`
- [x] 创建 `web/src/view/dataIntegration/dataSource.vue`
- [x] 创建 `web/src/view/dataIntegration/dataSourceForm.vue`

### Task 5: 配置菜单和路由
- [x] 更新 `web/src/pathInfo.json`
- [x] 添加 dataSource 和 dataSourceForm 到 sys_base_menus
- [x] 添加菜单权限到 sys_authority_menus

## Files Created/Modified

### Backend (new files)
- `server/model/system/sys_datasource.go` — DataSource model
- `server/service/system/sys_data_source.go` — DataSource service
- `server/api/v1/system/data_source.go` — DataSource API
- `server/router/system/data_source.go` — DataSource router

### Backend (modified files)
- `server/initialize/gorm.go` — Added SysDataSource to AutoMigrate
- `server/service/system/enter.go` — Added DataSourceService
- `server/api/v1/system/enter.go` — Added DataSourceApi
- `server/router/system/enter.go` — Added DataSourceRouter
- `server/initialize/router.go` — Registered DataSourceRouter

### Frontend (new files)
- `web/src/api/dataSource.js` — API wrapper
- `web/src/view/dataIntegration/dataSource.vue` — List page
- `web/src/view/dataIntegration/dataSourceForm.vue` — Form page

### Frontend (modified files)
- `web/src/pathInfo.json` — Added route mappings

### Database
- sys_base_menus: Added dataSource (id=15) and dataSourceForm (id=16)
- sys_authority_menus: Added menu authority for role 888

## Tasks

### Task 1: 创建后端数据模型

<read_first>
- server/model/system/sys_datasource.go — 数据源模型
- server/model/request/ — 请求模型目录结构
- server/service/ — 服务层目录
- server/api/v1/ — API层目录
</read_first>

<acceptance_criteria>
- server/model/system/auto_code.go 包含 DataSource 模型定义
- GORM auto-migrate 可创建 sys_datasource 表
</acceptance_criteria>

<action>
1. 创建 `server/model/system/sys_datasource.go`，包含字段：
   - ID (uint)
   - Name (string) — 数据源名称
   - Type (string) — MySQL/达梦/人大金仓/PostgreSQL/Oracle/SQLServer
   - Host (string) — 主机地址
   - Port (int) — 端口
   - Database (string) — 数据库名称
   - Username (string) — 用户名
   - Password (string) — 密码（加密存储）
   - Status (uint) — 0=禁用 1=启用
   - Remark (string) — 备注

2. 在 `server/model/system/auto_code.go` 中添加 DataSource 模型的 AutoMigrate 调用
</action>

### Task 2: 创建 Service 层

<read_first>
- server/service/system/ — 系统服务层实现
- server/service/enter.go — 服务组入口
</read_first>

<acceptance_criteria>
- service.ServiceGroupApp 包含 DataSourceService
- CreateDataSource, UpdateDataSource, DeleteDataSource, GetDataSourceList 方法存在
</acceptance_criteria>

<action>
1. 创建 `server/service/system/sys_data_source.go`，实现：
   - CreateDataSource(dataSource *model.DataSource) error
   - UpdateDataSource(dataSource *model.DataSource) error
   - DeleteDataSource(id uint) error
   - GetDataSourceList(info request.PageInfo) ([]model.DataSource, int64, error)
   - GetDataSourceById(id uint) (*model.DataSource, error)

2. 在 `server/service/enter.go` 的 ServiceGroup 中添加 DataSourceService
</action>

### Task 3: 创建 API 控制器

<read_first>
- server/api/v1/system/sys_user.go — API 实现参考
- server/router/system/sys_menu.go — 路由注册参考
</read_first>

<acceptance_criteria>
- server/api/v1/system/ 包含 data_source.go
- swagger 注释完整（@Tags, @Summary, @Security, @Param, @Success, @Router）
</acceptance_criteria>

<action>
1. 创建 `server/api/v1/system/data_source.go`，实现：
   - CreateDataSource — POST /dataSource/createDataSource
   - UpdateDataSource — POST /dataSource/updateDataSource
   - DeleteDataSource — POST /dataSource/deleteDataSource
   - GetDataSourceList — POST /dataSource/getDataSourceList
   - GetDataSourceById — POST /dataSource/getDataSourceById

2. 在 `server/router/system/` 注册路由
</action>

### Task 4: 创建前端页面

<read_first>
- web/src/view/dataIntegration/offlineSync.vue — 参考实现
- web/src/view/dataIntegration/offlineSyncForm.vue — 表单参考
- web/src/api/offlineSync.js — API 封装参考
</read_first>

<acceptance_criteria>
- web/src/view/dataIntegration/dataSource.vue 存在
- web/src/view/dataIntegration/dataSourceForm.vue 存在
- web/src/api/dataSource.js 存在
</acceptance_criteria>

<action>
1. 创建 `web/src/api/dataSource.js`，封装 API 调用

2. 创建 `web/src/view/dataIntegration/dataSource.vue`（列表页），包含：
   - 搜索栏：名称、类型、状态
   - 表格：名称、类型、主机、端口、数据库、状态、操作按钮
   - 分页组件
   - 新增、编辑、删除功能
   - 启用/禁用切换

3. 创建 `web/src/view/dataIntegration/dataSourceForm.vue`（表单页），包含：
   - 返回列表按钮
   - 表单：名称、类型（下拉选择）、主机、端口、数据库、用户名、密码、备注
</action>

### Task 5: 配置菜单和路由

<read_first>
- web/src/pathInfo.json — 路由名称映射
</read_first>

<acceptance_criteria>
- sys_base_menus 包含 dataSource 记录（parent_id=11）
- pathInfo.json 包含 dataSource 和 dataSourceForm 映射
</acceptance_criteria>

<action>
1. 在 MySQL sys_base_menus 表中插入数据源管理菜单：
   - name: dataSource
   - title: 数据源管理
   - parent_id: 11 (数据集成)
   - path: dataSource
   - component: view/dataIntegration/dataSource.vue
   - hidden: 0

2. 更新 `web/src/pathInfo.json`，添加：
   - "/src/view/dataIntegration/dataSource.vue": "dataSource"
   - "/src/view/dataIntegration/dataSourceForm.vue": "dataSourceForm"
</action>

## Expected Outcome

- 后端：DataSource 模型、Service、API、路由完整可用
- 前端：dataSource 列表页和 dataSourceForm 表单页完整
- 菜单：数据集成 > 数据源管理 显示在侧边栏
