---
phase: 06-offline-sync
plan: '06-01'
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/view/dataIntegration/index.vue
  - web/src/view/dataIntegration/offlineSync.vue
  - web/src/api/offlineSync.js
autonomous: false
requirements:
  - OFFLINE-SYNC-01

must_haves:
  truths:
    - "User can see offline sync task list with table display"
    - "User can search and filter sync tasks"
    - "User can trigger manual sync execution"
    - "User can view sync execution history/results"
  artifacts:
    - path: "web/src/view/dataIntegration/offlineSync.vue"
      provides: "Offline sync task management page with table"
      min_lines: 200
    - path: "web/src/api/offlineSync.js"
      provides: "API stubs for offline sync CRUD operations"
      exports: ["getOfflineSyncList", "createOfflineSync", "updateOfflineSync", "deleteOfflineSync", "triggerSync"]
    - path: "web/src/view/dataIntegration/index.vue"
      provides: "Container component for data integration menu"
---

<objective>
创建离线同步功能的基础页面结构（Vue + API stub），为后续完整的后端同步逻辑实现奠定基础。

Purpose: 为离线同步功能创建前端框架和数据模型约定，包括任务列表页面、API接口定义和菜单配置。
Output:
- web/src/view/dataIntegration/index.vue (容器页面)
- web/src/view/dataIntegration/offlineSync.vue (离线同步任务列表页面)
- web/src/api/offlineSync.js (API接口stub)
- 数据库菜单记录 (dataIntegration/offlineSync)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
<!-- 已从参考文件提取的模式 -->

来自 web/src/view/superAdmin/operation/sysOperationRecord.vue 的表格模式：
- 使用 el-table 展示数据，el-table-column 定义各列
- 搜索表单使用 el-form :inline="true" + el-form-item
- 分页使用 el-pagination 配合 page/pageSize/total
- 定义 name: 'SysOperationRecord' 供路由使用
- defineOptions({ name: 'ComponentName' }) 暴露组件名

来自 web/src/api/sysOperationRecord.js 的API模式：
- 每个API函数包含完整的Swagger注释
- 使用 service({ url, method, data/params }) 调用
- export const functionName = (params) => {}

来自 .planning/phases/05-01-SUMMARY.md 的菜单信息：
- dataIntegration 菜单已存在 (id=11, parent_id=0)
- 需要添加 offlineSync 子菜单 (parent_id=11)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create dataIntegration container component</name>
  <files>web/src/view/dataIntegration/index.vue</files>
  <action>
Create web/src/view/dataIntegration/index.vue as a simple container/wrapper component for the data integration menu section.

Structure:
- Template: Single router-view to render child pages
- Script: Minimal setup, defineOptions with name 'DataIntegration'
- Style: Basic container styling

This follows the pattern of other menu section containers in the project.
  </action>
  <verify>
    <automated>ls -la web/src/view/dataIntegration/index.vue</automated>
  </verify>
  <done>web/src/view/dataIntegration/index.vue created and accessible</done>
</task>

<task type="auto">
  <name>Task 2: Create offline sync API stubs</name>
  <files>web/src/api/offlineSync.js</files>
  <action>
Create web/src/api/offlineSync.js with API stub functions for offline sync operations.

Export these functions (each with Swagger comments):
1. getOfflineSyncList - GET /offlineSync/getOfflineSyncList (分页获取同步任务列表)
2. createOfflineSync - POST /offlineSync/createOfflineSync (创建同步任务)
3. updateOfflineSync - PUT /offlineSync/updateOfflineSync (更新同步任务)
4. deleteOfflineSync - DELETE /offlineSync/deleteOfflineSync (删除同步任务)
5. triggerSync - POST /offlineSync/triggerSync (手动触发同步执行)
6. getSyncHistory - GET /offlineSync/getSyncHistory (获取同步执行历史)

Follow the API pattern from sysOperationRecord.js - use service({ url, method, data/params }).
All endpoints are stubs returning mock data for now since backend is not implemented.
  </action>
  <verify>
    <automated>ls -la web/src/api/offlineSync.js && grep -c "export const" web/src/api/offlineSync.js</automated>
  </verify>
  <done>API stubs created with 6 exported functions covering CRUD + trigger + history</done>
</task>

<task type="auto">
  <name>Task 3: Create offline sync task list page</name>
  <files>web/src/view/dataIntegration/offlineSync.vue</files>
  <action>
Create web/src/view/dataIntegration/offlineSync.vue - the main offline sync task management page.

Page structure following sysOperationRecord.vue pattern:

1. Template:
   - Search box with form fields: task name, status (pending/running/success/failed), date range
   - Action buttons: Create Task (新增任务), Refresh (刷新)
   - el-table displaying sync tasks with columns:
     * Checkbox (selection)
     * Task Name (taskName)
     * Source Type (sourceType) - e.g., MySQL, PostgreSQL, API
     * Target Type (targetType) - e.g., MySQL, Elasticsearch, API
     * Status (status) - pending/running/success/failed with el-tag coloring
     * Last Sync Time (lastSyncTime)
     * Next Sync Time (nextSyncTime) - for scheduled tasks
     * Operations (编辑, 删除, 触发)
   - el-drawer for create/edit task form
   - el-dialog for sync history view
   - Pagination (el-pagination)

2. Script (setup script):
   - Import API functions from @/api/offlineSync
   - Import formatDate from @/utils/format
   - defineOptions({ name: 'OfflineSync' })
   - Refs: tableData, searchInfo, page, pageSize, total, dialogFlag, drawerTitle
   - getTableData() - fetch task list from API
   - handleSizeChange(), handleCurrentChange() - pagination
   - onSubmit(), onReset() - search
   - handleSelectionChange() - multi-select
   - openDrawer() - open create/edit drawer
   - saveTask() - submit create/edit form
   - deleteTask() - delete single task
   - triggerSync() - manually trigger sync execution
   - viewHistory() - view sync execution history

3. Task data model (for mock/stub):
   - id: number
   - taskName: string
   - sourceType: string (MySQL/PostgreSQL/API/File)
   - sourceConfig: object (connection config)
   - targetType: string (MySQL/Elasticsearch/API/File)
   - targetConfig: object
   - mappingRules: object (field mapping)
   - status: string (pending/running/success/failed)
   - cronExpression: string (optional, for scheduled)
   - lastSyncTime: datetime
   - nextSyncTime: datetime
   - createdAt: datetime

4. Mock data:
   - Include 5-6 sample tasks with varied statuses for demonstration
   - This is a UI stub - real sync logic comes later

Use el-tag with type colors:
- success: green (completed)
- warning: yellow (running)
- info: gray (pending)
- danger: red (failed)
  </action>
  <verify>
    <automated>ls -la web/src/view/dataIntegration/offlineSync.vue && wc -l web/src/view/dataIntegration/offlineSync.vue</automated>
  </verify>
  <done>Offline sync page displays task table with search, pagination, create/edit drawer, and trigger functionality</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verify offline sync page and add menu</name>
  <what-built>
    离线同步功能基础结构已创建：
    - dataIntegration/index.vue (容器)
    - dataIntegration/offlineSync.vue (任务列表页面)
    - api/offlineSync.js (API stubs)
  </what-built>
  <how-to-verify>
    1. 检查文件是否创建成功:
       ls web/src/view/dataIntegration/
       ls web/src/api/offlineSync.js

    2. 启动前端开发服务器:
       cd web && npm run dev

    3. 访问页面验证:
       - 导航到 "数据集成模版" 菜单
       - 应该看到 "离线同步" 子菜单
       - 点击进入离线同步页面
       - 验证表格、搜索、分页功能正常显示
       - 测试新建任务抽屉、触发同步按钮

    4. 检查浏览器控制台无错误
  </how-to-verify>
  <resume-signal>
    Type "approved" or describe issues found
  </resume-signal>
</task>

<task type="auto">
  <name>Task 5: Insert offlineSync menu record</name>
  <files>server/source/system/menu.go</files>
  <action>
Insert the offlineSync menu record into the database via SQL. This must be done after user approves the page.

SQL to execute:
```sql
INSERT INTO sys_base_menus (id, parent_id, path, name, component, menu_level, sort, title, icon, hidden, keep_alive, default_menu, active_name, close_tab, transition_type)
VALUES (12, 11, 'offlineSync', 'offlineSync', 'view/dataIntegration/offlineSync.vue', 1, 1, '离线同步', 'sync', 0, 1, 0, '', 0, 1);

-- Also ensure the menu is visible to authority 888
INSERT INTO sys_authority_menus (sys_base_menu_id, sys_authority_authority_id) VALUES (12, 888);
```

Note: menu id 12 because current max is 11 from Phase 5.
  </action>
  <verify>
    <automated>mysql -h 127.0.0.1 -P 13306 -u gva -p'Aa@6447985' qmPlus -e "SELECT * FROM sys_base_menus WHERE path='offlineSync'"</automated>
  </verify>
  <done>offlineSync menu record inserted with id=12, parent_id=11, visible in UI</done>
</task>

</tasks>

<verification>
- web/src/view/dataIntegration/ directory exists with index.vue and offlineSync.vue
- web/src/api/offlineSync.js exists with 6 exported API functions
- Offline sync page displays in browser without console errors
- Menu item "离线同步" appears under "数据集成模版" in sidebar
</verification>

<success_criteria>
1. Data integration container component renders child pages via router-view
2. Offline sync page shows task table with mock data (5-6 sample tasks)
3. Search form filters tasks by name and status
4. Pagination works correctly
5. Create/Edit drawer opens and has form fields for task configuration
6. Trigger sync button is visible and shows confirmation
7. offlineSync menu appears in sidebar under dataIntegration
8. Page loads without JavaScript errors in browser console
</success_criteria>

<output>
After completion, create `.planning/phases/06-offline-sync/06-01-SUMMARY.md`
</output>
