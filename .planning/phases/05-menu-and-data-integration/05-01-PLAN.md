---
phase: 05-menu-and-data-integration
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements:
  - "MENU-01"
  - "MENU-02"
must_haves:
  truths:
    - "审计日志菜单显示在超级管理员下"
    - "数据集成模版作为一级菜单显示"
  artifacts:
    - path: "sys_base_menus"
      provides: "菜单数据库记录"
      contains: "id=10 (operation), id=11 (dataIntegration)"
  key_links:
    - from: "sys_base_menus"
      to: "sys_base_menus"
      via: "parent_id reference"
      pattern: "parent_id=2 for operation, parent_id=0 for dataIntegration"
---

<objective>
在数据库中插入两条菜单记录：
1. 恢复审计日志子菜单（挂载在超级管理员下 parent_id=2）
2. 新增数据集成模版一级菜单（parent_id=0）
</objective>

<context>
@web/src/view/superAdmin/operation/sysOperationRecord.vue
@web/src/pathInfo.json

当前菜单结构（sys_base_menus）:
| id | parent_id | path        | title     | sort |
|----|-----------|-------------|-----------|------|
| 1  | 0         | dashboard   | 仪表盘    | 1    |
| 2  | 0         | superAdmin  | 超级管理员| 3    |
| 3  | 0         | person      | 个人信息  | 4    |
| 4  | 0         | systemTools | 系统工具  | 5    |
| 5  | 2         | user        | 用户管理  | 4    |
| 6  | 2         | authority   | 角色管理  | 1    |
| 7  | 2         | menu        | 菜单管理  | 2    |
| 8  | 2         | api         | API管理   | 3    |
| 9  | 4         | autoCode    | 代码生成器| 1    |
</context>

<tasks>

<task type="auto">
  <name>Task 1: 插入审计日志菜单记录</name>
  <action>
    连接 MySQL 数据库 (127.0.0.1:13306, qmPlus) 执行以下 SQL:

    ```sql
    INSERT INTO sys_base_menus (
      id, parent_id, path, `name`, component, menu_level,
      sort, title, icon, hidden, keep_alive, default_menu,
      active_name, close_tab, transition_type
    ) VALUES (
      10, 2, 'operation', 'operation', 'view/superAdmin/operation/sysOperationRecord.vue',
      1, 5, '操作记录', 'operation', 0, 1, 0, '', 0, 1
    );
    ```

    字段说明:
    - id=10: 下一个可用ID
    - parent_id=2: 挂载在超级管理员下
    - path=operation: 路由路径
    - component: 指向已有的 Vue 文件
    - menu_level=1: 子菜单级别
    - sort=5: 在超级管理员下排序（当前 max=4）
    - hidden=0: 显示
    - keep_alive=1: 保持活力
    - transition_type=1: 过渡动画类型
  </action>
  <verify>
    <automated>
      mysql -h 127.0.0.1 -P 13306 -u gva -p'Aa@6447985' qmPlus -e "SELECT id, parent_id, path, title FROM sys_base_menus WHERE id=10"
    </automated>
  </verify>
  <done>审计日志菜单记录已插入，parent_id=2, path=operation, title=操作记录</done>
</task>

<task type="auto">
  <name>Task 2: 插入数据集成模版一级菜单记录</name>
  <action>
    连接 MySQL 数据库 (127.0.0.1:13306, qmPlus) 执行以下 SQL:

    ```sql
    INSERT INTO sys_base_menus (
      id, parent_id, path, `name`, component, menu_level,
      sort, title, icon, hidden, keep_alive, default_menu,
      active_name, close_tab, transition_type
    ) VALUES (
      11, 0, 'dataIntegration', 'dataIntegration', 'view/dataIntegration/index.vue',
      0, 6, '数据集成模版', 'dataIntegration', 0, 1, 0, '', 0, 1
    );
    ```

    字段说明:
    - id=11: 下一个可用ID
    - parent_id=0: 一级菜单
    - path=dataIntegration: 路由路径（约定）
    - component: view/dataIntegration/index.vue（页面暂不存在，先建路由记录）
    - menu_level=0: 一级菜单级别
    - sort=6: 在一级菜单中排序（在 systemTools 之后）
    - hidden=0: 显示
    - keep_alive=1: 保持活力
    - transition_type=1: 过渡动画类型
  </action>
  <verify>
    <automated>
      mysql -h 127.0.0.1 -P 13306 -u gva -p'Aa@6447985' qmPlus -e "SELECT id, parent_id, path, title FROM sys_base_menus WHERE id=11"
    </automated>
  </verify>
  <done>数据集成模版菜单记录已插入，parent_id=0, path=dataIntegration, title=数据集成模版</done>
</task>

</tasks>

<verification>
1. 查询验证审计日志菜单存在:
   ```bash
   mysql -h 127.0.0.1 -P 13306 -u gva -p'Aa@6447985' qmPlus -e "SELECT * FROM sys_base_menus WHERE path='operation' AND parent_id=2"
   ```

2. 查询验证数据集成模版菜单存在:
   ```bash
   mysql -h 127.0.0.1 -P 13306 -u gva -p'Aa@6447985' qmPlus -e "SELECT * FROM sys_base_menus WHERE path='dataIntegration' AND parent_id=0"
   ```

3. 查询完整菜单树:
   ```bash
   mysql -h 127.0.0.1 -P 13306 -u gva -p'Aa@6447985' qmPlus -e "SELECT id, parent_id, path, title, sort FROM sys_base_menus ORDER BY parent_id, sort"
   ```
</verification>

<success_criteria>
- [ ] sys_base_menus 表中 id=10 的记录存在，parent_id=2, path=operation, title=操作记录
- [ ] sys_base_menus 表中 id=11 的记录存在，parent_id=0, path=dataIntegration, title=数据集成模版
- [ ] 两条记录的 hidden=0（菜单可见）
- [ ] 可以通过 MySQL 查询确认插入成功
</success_criteria>

<output>
After completion, create `.planning/phases/05-menu-and-data-integration/05-01-SUMMARY.md`
</output>
